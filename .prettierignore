**/node_modules
**/.svelte-kit
**/build
**/.gradle
**/dist
**/deno.lock
target
crates
**/src-tauri/gen
# The inlang-generated paraglide tree: the generator rewrites it on every
# compile, so formatting it is never durable (its own nested ignore is not
# honored when prettier runs from the repo root).
packages/kallip-ui/src/paraglide
# Markdown is formatted by rumdl (see .rumdl.toml), not prettier -- prettier
# pads/aligns table columns, which reflows every row on a one-line edit.
**/*.md
