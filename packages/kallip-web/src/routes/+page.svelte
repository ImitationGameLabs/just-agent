<script lang="ts">
  // The root `/` is never rendered: the gate redirects online users to
  // /tagmata before this page mounts. (The offline product moved to the
  // kallip-direct package and is not reachable from this shell.) The
  // route exists so SvelteKit has a page for the layout to wrap during the
  // (skeleton) resolving window.
</script>

<div class="p-4"></div>
